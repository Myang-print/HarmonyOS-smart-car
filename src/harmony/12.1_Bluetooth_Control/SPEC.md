# Iter1 规格

Iter1要求手机发送`W/A/S/D/O`后，经Hi3861转发控制STM32完成前进、左转、后退、右转和停止。

开发侧验收包括：协议单元测试通过、根与模块`BUILD.gn`只启用本功能、Hi3861全量构建成功、STM32 Keil全量构建零错误零警告。硬件分层诊断要求：蓝色间断闪烁表示STM32尚未收到合法帧；白色常亮表示Hi3861到STM32链路已通；手机发送合法字符后小车产生对应动作。成功基线不向手机返回初始化信息或ACK，因此手机接收区为空不判定失败。硬件侧最终验收包括两端烧录、手机连接、架空动作和停止测试；硬件验收不能由编译替代。
