# TableGuard 桌面防跌落

GPIO13、GPIO14读取左右红外传感器，上电静止标定桌面电平。连续检测到边缘后执行“停车 → 后退 → 向安全方向原地转向 → 停车”，传感器读取失败时立即停车。

电机命令经 GPIO11/UART2 发送到 STM32 PA10，串口为115200-8-N-1，帧格式为 `FC 左方向 左速度 右方向 右速度 FD`。该示例与 `12.0_UART_Correspondence` 竞争同一个UART2，构建时只能启用一个。
