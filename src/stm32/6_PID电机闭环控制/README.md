# 编码器PI闭环与路径动作

在TIM4电机PWM之上加入TIM2/TIM3编码器反馈、增量PI、左右轮同步修正和前馈。提供前进、后退、编码器定距90°左右转接口，并用WS2812显示当前运动状态。

`USER/main.c`执行一条闭合测试路径：前进、右转、前进、左转、后退、右转、后退、左转。转角由 `control_system.h` 中左右90°目标计数分别标定；Keil构建通过不代表实车角度已经准确。
