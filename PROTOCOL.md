# 智能小车接口与协议基线

> 本文是后续“黑胶带循迹 + 岔路选择 + 死胡同回退 + 终点停车”开发的接口基线。结论来自当前源码与构建入口的逐项核对；尚未经过本轮实车测量的内容明确标为“待实机确认”。

## 1. 目标架构与职责边界

```text
黑胶带/岔路/终点传感器 ─┐
HC-SR04 距离传感器     ─┼─> Hi3861：采样、滤波、路口判定、路径栈/回溯、终点判定
其他上层输入           ─┘             │
                                     │ UART2：左右轮目标速度（周期刷新）
                                     v
                          STM32F103：验帧、失联停车、编码器 PI、PWM、电机/灯效
```

必须遵守：

1. Hi3861 是唯一的自主决策者；STM32 只执行左右轮速度并提供本地失联保护。不要同时在两端维护两套迷宫状态机。
2. 后续综合功能以现有 **6 字节左右轮速度协议** 为执行链路基线，不采用旧避障工程的 5 字节离散动作协议。
3. `src/harmony/BUILD.gn` 一次只能启用一个占用 UART2/车控权的业务 Feature。循迹功能应新增独立 Feature，并停用 `12.1_Bluetooth_Control`、`12.0_UART_Correspondence`、`2.1_TableGuard` 等竞争者。
4. STM32 配对基线应从 `src/stm32/7.1_Bluetooth_Control` 或 `src/stm32/7_串口通信` 选一个复制/演进；两者当前核心车控接口相同。不得把 `41_Autonomous_Obstacle_Avoidance` 与该链路混烧。
5. 上电、初始化失败、传感器无效、协议非法、通信超时、终点确认时均应下发/保持 `(0, 0)`，安全优先于继续行驶。

## 2. 推荐使用的主车控链路

### 2.1 物理连接与串口参数

| 信号 | Hi3861 | STM32F103 | 要求 |
| --- | --- | --- | --- |
| 车控 TX → RX | GPIO11 / UART2_TX | PA10 / USART1_RX | 必须共地，3.3 V TTL |
| 可选反向通道 | GPIO12 / UART2_RX | PA9 / USART1_TX | 当前协议不需要；接入前防止日志污染协议 |
| 串口格式 | UART2 | USART1 | `115200-8-N-1`，无硬件流控 |

当前 Hi3861 调用：

```c
GpioInit();
IoSetFunc(WIFI_IOT_IO_NAME_GPIO_11,
          WIFI_IOT_IO_FUNC_GPIO_11_UART2_TXD);
IoSetFunc(WIFI_IOT_IO_NAME_GPIO_12,
          WIFI_IOT_IO_FUNC_GPIO_12_UART2_RXD); /* 仅在需要 RX 时配置 */
UartInit(WIFI_IOT_UART_IDX_2, &attribute, NULL);
UartWrite(WIFI_IOT_UART_IDX_2, frame, 6);
```

当前 STM32 调用关系：

```text
main
 ├─ uart_init(115200)                         // PA10 RXNE 中断
 ├─ Control_System_Init()                    // TIM4 PWM + TIM2/TIM3 编码器
 └─ 每 50 ms：
     ├─ UART_TryReadMotorCommand()
     ├─ Control_SetRemoteTarget(left, right)  // 收到合法帧时刷新目标/超时计时
     └─ Control_RemoteStep()                  // PI、PWM、灯效、2 s 失联停车

USART1_IRQHandler
 └─ UART_ParseMotorByte()                    // 字节流重同步、验帧、发布最新完整帧
```

### 2.2 6 字节左右轮速度帧（主协议）

固定长度 6 字节，不是 ASCII 文本：

| 偏移 | 字段 | 合法值 | 含义 |
| ---: | --- | --- | --- |
| 0 | `HEADER` | `0xFC` | 帧头，同时用于接收器重新同步 |
| 1 | `LEFT_DIR` | `0` / `1` | 0 正转（车体前进方向），1 反转 |
| 2 | `LEFT_SPEED` | `0..250` | 左轮速度绝对值，单位 `0.01 rad/s` |
| 3 | `RIGHT_DIR` | `0` / `1` | 0 正转，1 反转 |
| 4 | `RIGHT_SPEED` | `0..250` | 右轮速度绝对值，单位 `0.01 rad/s` |
| 5 | `TAIL` | `0xFD` | 帧尾 |

编码公式：

```text
dir   = signed_speed < 0 ? 1 : 0
speed = min(abs(signed_speed), 250)
frame = FC dirL speedL dirR speedR FD
```

标准动作示例（数值仅是协议示例，循迹速度需实车标定）：

| 动作 | 有符号目标 `(L, R)` | 帧示例 |
| --- | --- | --- |
| 停车 | `(0, 0)` | `FC 00 00 00 00 FD` |
| 前进 1.00 rad/s | `(100, 100)` | `FC 00 64 00 64 FD` |
| 后退 1.00 rad/s | `(-100, -100)` | `FC 01 64 01 64 FD` |
| 原地左转 | `(-50, 150)` | `FC 01 32 00 96 FD` |
| 原地右转 | `(150, -50)` | `FC 00 96 01 32 FD` |

接收约束：

- STM32 遇到任意 `0xFC` 都从新帧头重新同步；这依赖方向只有 `0/1` 且速度最大 `250 (0xFA)`，负载不会出现 `0xFC`。
- 方向字段超出 `0/1`、速度大于 `250` 或尾字节不是 `0xFD` 时，整帧无效并立即停车/报告协议错误。
- 新合法帧会覆盖尚未由主循环消费的旧帧；本协议表达“当前目标”，不表达必须逐条执行的动作队列。
- 当前帧 **没有长度、版本、序号、校验和或 ACK**。它适合短线、板间、周期刷新的目标速度，不可据此确认小车已实际完成动作。

### Hi3861→STM32 动作链路已确认

- Iter1 诊断固件能够完成 UART2/GPIO11 初始化和固定动作序列发送；
- 实车已观察到前进、后退、左转、右转；
- 每个动作后的停车和最终停车均有效；
- 未发现异味或异常声音；发热程度尚未确认；
- 2 秒失联停车已通过独立实车测试：Hi3861 只发送一次低速前进帧后静默 4 秒，车轮先转动并在约 2 秒后自动停止，之后保持停止。

## 2.3 刷新、调度和失联安全

- STM32 `Control_RemoteStep()` 必须每 `CAR_CONTROL_PERIOD_MS = 50 ms` 调用一次。
- STM32 从收到合法帧起计时，`CAR_REMOTE_COMMAND_TIMEOUT_MS = 2000 ms` 后没有新合法帧则停车。
- Hi3861 当前蓝牙基线每 `100 ms` 刷新一次；轨迹示例每 `500 ms` 刷新一次。后续循迹建议固定 **50–100 ms** 刷新，不得超过 500 ms，以留出调度抖动余量。
- 车控发送任务不得被阻塞式传感器读取长期饿死。现有成功基线将 `motor_tx` 设为优先级 25、阻塞式 `bluetooth_rx` 设为 24；综合应用应同样让固定周期发送/安全任务拥有更高优先级，或保证所有采样有严格超时。
- `UartWrite()` 必须检查返回值是否等于 6；短写或失败后立即尝试发送停车帧并进入故障状态。
- 启动任务前先发停车帧；任务创建失败也必须保持停车。

## 3. STM32 电机与闭环接口

当前 `7.1_Bluetooth_Control` / `7_串口通信` 的硬件映射：

| 功能 | STM32 资源 |
| --- | --- |
| 左轮方向 | PB14 / L-IB |
| 左轮 PWM | PB7 / TIM4_CH2 / L-IA |
| 右轮方向 | PB13 / R-IA |
| 右轮 PWM | PB6 / TIM4_CH1 / R-IB |
| 左编码器 | TIM2 |
| 右编码器 | TIM3 |
| PWM | TIM4，1 kHz（72 MHz / 10 / 7200） |
| 闭环周期 | 50 ms |

控制入口：

- 自主运行时使用 `Control_SetRemoteTarget(s16 left_speed_x100, s16 right_speed_x100)`，随后周期调用 `Control_RemoteStep()`。
- `Control_SetWheels()` 是底层 PWM 接口，不应由 Hi3861 协议解析路径绕过 PI 随意调用。
- 当前协议速度到编码器目标存在 `5/2` 巡航增益；因此字段值不是未经修正的实测轮速承诺，循迹速度和转弯参数必须实车标定。
- 当前左右编码器前进符号均配置为 `+1`，每轮约 `2800` 计数；这些是当前工程常量，换电机/编码器后必须重验。
- 现有 `Control_TurnLeft90()` / `Control_TurnRight90()` 是 STM32 本地阻塞式编码器动作接口，不在 6 字节协议中暴露。后续若由 Hi3861 完成迷宫决策，应优先通过差速目标控制转向，避免混合“持续速度命令”和“本地阻塞动作”。

## 4. 黑胶带传感器接口基线

当前赛道事实（用户描述 + `figures/` 图示确认）：

- 目标路线是黑胶带；两侧白胶带是道路包边。
- 小车底部两个探头的整体横向覆盖宽度略大于中间黑胶带宽度；尚未获得厘米尺寸或探头中心间距。
- 起点和终点各自都是一个整体“干”字型标记：一条沿行驶方向连续的纵向黑线，叠加两条互相平行且垂直于路径的横向黑线；两条横线之间有白色间隔，但纵向黑线在间隔中继续。两条横线不能分别解释为起点和终点。
- 本次赛道的固定选择策略由用户确定：第一个 Y 字岔路选择左侧轨道，第二个 Y 字岔路选择右侧轨道。该策略仅适用于本次赛道布局，不抽象为通用左手优先或右手优先规则。
- 死路按赛道规则表现为一条垂直于当前路径的单条黑色横带；由于照片中的横带与主线存在连通/透视歧义，程序不能仅凭静态图片假设它一定已经识别成功，必须在实车时序测试中确认。

仓库当前没有完成的“黑线循迹/岔路识别”模块；可复用的是 `2.1_TableGuard` 的双路数字红外 GPIO 读取方式：

| 逻辑位置 | Hi3861 GPIO | 当前接口 |
| --- | ---: | --- |
| 左红外 | GPIO13 | `GpioGetInputVal(13, &left)` |
| 右红外 | GPIO14 | `GpioGetInputVal(14, &right)` |

- `L=0,R=0`：两个探头在黑线两侧白色包边，正常居中前进；
- `L=1,R=0`：左探头检测黑胶带，执行左侧差速修正；
- `L=0,R=1`：右探头检测黑胶带，执行右侧差速修正；
- `L=1,R=1`：一个探头检测黑胶带、另一个探头越过白色包边，或两个探头进入特殊宽黑区域；旧版本立即停车。修正版在已有有效运动状态后最多保持最近动作 8 个采样（约 800 ms），仍未恢复则停车；上电尚无有效运动状态时立即停车。

该极性是当前这套传感器和接线的实测协议，后续更换传感器、调整安装或修改接线后必须重新验证。

初始化顺序：

```c
GpioInit();
IoSetFunc(WIFI_IOT_IO_NAME_GPIO_13, WIFI_IOT_IO_FUNC_GPIO_13_GPIO);
IoSetFunc(WIFI_IOT_IO_NAME_GPIO_14, WIFI_IOT_IO_FUNC_GPIO_14_GPIO);
GpioSetDir(13, WIFI_IOT_GPIO_DIR_IN);
GpioSetDir(14, WIFI_IOT_GPIO_DIR_IN);
```

要求与未决项：

1. 当前这套传感器和接线的电平语义已经由 Iter0 实机反馈确认：`0` 表示探头下方为白色道路包边；`1` 表示探头下方为黑胶带或已经越过白色道路包边。该结论不是通用传感器规则，硬件更换、安装变化或接线变化后必须重新验证。
2. 由于两个探头的整体横向覆盖宽度略大于中间黑胶带，静态双路状态不能直接等价为“已选择左支路/右支路”。Y 字路口必须使用减速、连续采样、运动过程和离开路口后的持续黑线跟踪进行确认；`11` 的短时保持规则仅是当前诊断版本的过渡策略，不是路口识别规则。
3. 双路传感器足以做基本偏差纠正，但通常无法仅凭单次采样无歧义地区分直线、Y 字分支、死路横带和“干”字型起终点标记。正式算法必须采用事件时序和状态锁存；如果实测日志显示信息仍不足，必须暂停算法开发并请求增加传感器或改变标记。
4. 输入应消抖/滤波。现有基线控制周期为 20 ms、连续 3 个样本确认事件，可作为起点；岔路与终点还需要“进入标记—居中—离开标记”的状态锁存，避免同一路口重复入栈。
5. 读取失败立即停车，不能沿用上一次“安全”读数无限行驶。
6. GPIO13/14 与 UART2 GPIO11/12 不冲突；新增传感器前必须再次检查与 UART、I2C、PWM 及板载器件的复用冲突。

## 5. HC-SR04 距离接口（可选安全传感器）

现有 `4.1_Hcsr04_Obstacle_Avoidance` 驱动可复用到 Hi3861 综合应用，但只复用测距驱动，不复用其旧车控 UART：

| 信号/参数 | 当前值 |
| --- | --- |
| Trig | GPIO7，GPIO 输出 |
| Echo | GPIO8，GPIO 输入 |
| 触发脉冲 | 高电平 12 µs（此前先低 2 µs） |
| 等待边沿超时 | 每阶段 30 ms |
| 合法量程 | 2–400 cm |
| 单次结果 | 最多尝试 5 次，取最多 3 个有效值的中位数 |
| 样本间隔 | 60 ms |
| 距离公式 | `echo_us * 0.0343 / 2` cm |

调用关系：

```text
Hcsr04_Init()
Hcsr04_ReadMedian(&distance_cm)
 └─ ReadOnce()
     ├─ 产生 Trig 脉冲
     ├─ WaitForLevel(HIGH, 30000 us)
     ├─ WaitForLevel(LOW, 30000 us)
     └─ 计算并检查 2..400 cm
```

注意：一次中位数读取在失败情况下可能阻塞数百毫秒，不能放在高优先级车控发送任务中。测距应独立任务运行并发布带时间戳的结果；结果过期或连续失败时停车。Echo 若为 5 V 输出，必须经分压/电平转换后进入 3.3 V Hi3861（电气电平待实机接线确认）。

## 6. 旧 5 字节离散动作协议（仅兼容性记录，禁止混用）

该协议只配对：

```text
src/harmony/4.1_Hcsr04_Obstacle_Avoidance
    GPIO6 / UART1_TX
        → STM32 PB8 / 软件 UART RX
src/stm32/41_Autonomous_Obstacle_Avoidance
```

参数为 `2400-8-N-1`，帧格式：

```text
A5 5A SEQ CMD XOR
XOR = A5 ^ 5A ^ SEQ ^ CMD
CMD ∈ {'F', 'S', 'L', 'R'}
```

Hi3861 每次把同一序号帧重复 3 次后序号加一；STM32 在 1000 ms 无合法帧时停车。STM32 实际解析器目前校验同步字、命令和 XOR，但 **没有拒绝重复/乱序 SEQ**，因此序号只是校验输入，不具备去重或顺序保证。

此链路与主协议在 GPIO、UART、波特率、帧长、语义和 STM32 固件上全部不同。尤其：

- 不能把 GPIO6/2400 的发送端接给 PA10/115200 的接收端；
- 不能把 `A5 5A...` 发给 `FC...FD` 解析器；
- 工作区中的 `41_Autonomous_Obstacle_Avoidance` 当前处于被删除状态，虽可从 Git `HEAD` 审计历史源码，但不能作为当前可构建目录。

## 7. 迷宫/回溯层的强制语义

6 字节协议只传“当前左右轮目标”，不能替代路径算法。Hi3861 后续至少需要以下状态与不变量：

```text
CALIBRATING → FOLLOW_LINE → APPROACH_JUNCTION → CENTER_ON_JUNCTION
            → CHOOSE_BRANCH → EXECUTE_TURN → LEAVE_JUNCTION → FOLLOW_LINE
FOLLOW_LINE / LEAVE_JUNCTION → DEAD_END → U_TURN → BACKTRACK
任意状态 → SENSOR_FAULT / LINK_FAULT / FINISHED → STOPPED
```

1. **岔路记录**：到达一个新岔路并居中后，记录可选出口、进入方向、已尝试出口和选择结果；不能只记录“左/右”而忽略车辆朝向。
2. **选择策略**：本次赛道不是通用左手或右手规则：第一个 Y 字岔路必须选择左侧轨道，第二个 Y 字岔路必须选择右侧轨道。程序应使用已确认的岔路序号选择，不得在运行时随机选择或依据瞬时 `L/R` 状态猜测。
3. **死胡同**：确认前方及左右均无线后停车，执行 180° 掉头；沿原路径返回到最近仍有未探索出口的岔路，弹出无剩余出口的栈项，再选择下一出口。
4. **原路返回**：至少需要拓扑栈和相对方向变换。仅按时间倒车不能可靠回到岔路；转向/路段完成应由胶带事件与编码器/超时共同约束。
5. **终点**：起点和终点都使用完整“干”字型标记。程序不得把第一条横线或第二条横线单独判为终点；必须观察到“普通路径 → 第一横线扩宽事件 → 中间纵线/白色间隔阶段 → 第二横线扩宽事件 → 离开完整标记”的时序，并结合运行阶段区分起点与终点。确认终点后清空运动意图，持续发送停车帧；不得在超时后自动恢复。
6. **事件去重**：同一物理岔路从检测到离开期间只允许处理一次；再次检测必须经过明确的离开条件。
7. **动作超时**：循线、居中、转向、U 形转弯和回溯各自必须有最大时长；超时进入故障停车，禁止无限转圈。

## 8. 当前接口能力缺口

在写迷宫算法前需接受或补齐以下缺口：

- 主链路是单向、无 ACK；Hi3861 无法知道 STM32 是否收到帧、编码器是否运动、转向是否完成或电机是否堵转。
- 主帧无校验和与序号；噪声导致的、碰巧字段合法的错误帧无法检出。
- 当前 STM32 只接收速度目标，不回传编码器里程、故障、当前状态。
- 双路红外不足以稳健识别所有岔路/终点形态。
- 当前仓库没有地图节点 ID 或绝对定位来源；“回到同一岔路”只能依赖拓扑次序与地面标记事件。

后续若需要可靠动作完成闭环，应新增**版本化协议**，而不是悄悄改变现有 6 字节帧：建议增加长度、消息类型、序号、payload、CRC、ACK/状态、编码器计数和故障码，并保留独立急停/超时规则。协议升级前需先同时实现两端测试向量，再切换固件。

## 9. 构建与配对规范

### Hi3861

1. 本仓库 `src/harmony/` 是 OpenHarmony `applications/sample/wifi-iot/app/` 内容快照，不是完整源码树。
2. 将根 `BUILD.gn` 与目标示例同步到完整源码树对应目录。
3. `src/harmony/BUILD.gn` 一次只启用一个 Feature；Iter0 硬件诊断时使用 `17_Iter0_Sensor_Diagnostic:iter0_sensor_diagnostic`，测试结束后再切回业务 Feature。
4. 使用对应 OpenHarmony 版本运行 `python build.py wifiiot`。
5. 日志出现构建成功只证明编译链接，不证明 GPIO 复用、UART 波形和传感器极性正确。

### STM32

1. 在配对工程 `USER/Template.uvprojx` 中 Rebuild。
2. 烧录由当前源码生成的 `OBJ/Template.hex`，不要复用同名旧产物。
3. 构建日志零错误不代表轮向、编码器符号、速度、转角和链路实测通过。

## 10. 开发前逐条验收清单

### 静态一致性

- [ ] Hi3861 与 STM32 都是 115200、8-N-1、无流控。
- [ ] 实际接线是 GPIO11 TX → PA10 RX，并且两板共地。
- [ ] 帧头 `FC`、帧尾 `FD`、方向 `0/1`、速度 `0..250` 两端一致。
- [ ] Hi3861 检查 `UartWrite == 6`，STM32 只发布完整合法帧。
- [ ] 发送刷新周期不超过 500 ms；STM32 50 ms 控制循环和 2000 ms 超时仍启用。
- [ ] `src/harmony/BUILD.gn` 没有同时启用竞争 UART/GPIO/车控权的 Feature。
- [ ] 红外、HC-SR04 与 UART 的 GPIO 无复用冲突。
- [ ] 终点、死胡同、岔路、传感器故障均存在明确停车路径。

### 台架与实车（本轮未执行）

- [ ] 架空车轮，上电时两轮不动。
- [ ] 逻辑分析仪确认 GPIO11 为 115200-8-N-1，停车帧为 `FC 00 00 00 00 FD`。
- [ ] 分别发送前进、后退、左转、右转，核对轮向及编码器正负。
- [ ] 停止 Hi3861 发送，确认 STM32 在约 2 s 内停车。
- [ ] 注入错误方向、超速和错误帧尾，确认 STM32 拒绝并停车。
- [ ] 在白底、黑带、岔路、断线和终点图案上记录每路传感器原始电平。
- [ ] 测量 HC-SR04 Echo 电压，确认不超过 Hi3861 输入允许范围。
- [ ] 标定循迹速度、差速增益、路口居中距离、90°/180°转向及动作超时。
- [ ] 分别验证单岔路、连续岔路、嵌套死胡同、回到已有岔路和终点永久停车。

## 12. Iter1 直线循迹 FSM（当前候选实现）

上一版 `SPIN_RECOVERY` 已在实车测试中失败：出现反向移动、不连续原地转向，且没有可靠恢复路径。当前候选固件改为全前进边缘跟随，不再发送反向或原地旋转帧。

规则：

- `00`：双轮正转直行。
- `10`：双轮正转，左轮慢、右轮快，向左修正。
- `01`：双轮正转，左轮快、右轮慢，向右修正。
- `11`：视为边缘/宽黑区域歧义，保持最近一次前进命令；若没有历史状态则发送正向直行。
- 只有传感器读取失败或输入值非法时停车。
- 固件不得因 `11` 停车、倒车或原地旋转；当前版本没有自动恢复超时。

这是针对普通直线的全前进候选，不是完整赛道算法；Y 字选择、死路回退和“干”字型终点识别仍未接入。


| 结论 | 当前源码 |
| --- | --- |
| 6 字节编码及动作速度 | `src/harmony/12.1_Bluetooth_Control/bluetooth_protocol.c` |
| UART2 初始化、100 ms 刷新、任务优先级 | `src/harmony/12.1_Bluetooth_Control/bluetooth_control.c` |
| 500 ms 周期轨迹发送 | `src/harmony/12.0_UART_Correspondence/correspondence.c` |
| GPIO13/14 红外读取与标定 | `src/harmony/2.1_TableGuard/TableGuard.c` |
| GPIO7/8 超声波及中位数读取 | `src/harmony/4.1_Hcsr04_Obstacle_Avoidance/src/hcsr04_driver.c` |
| Iter0 红外诊断固件与操作单 | `src/harmony/17_Iter0_Sensor_Diagnostic/`、`tests/hardware/` |
| Iter0→Iter1 前置数据与退出门禁 | `tests/hardware/ITER0-EXIT-CRITERIA.md` |
| Iter0 赛道几何与时序采样 | `tests/hardware/ITER0-track-geometry.md`、`tests/hardware/ITER0-track-sequence-TEST.md` |
| Iter0 手推日志解析报告 | `tests/hardware/ITER0-drive-log-analysis-01.md` |
| Iter0 最后两组补测 | `tests/hardware/ITER0-final-two-samples-TEST.md` |
| Iter0 补测日志解析报告 | `tests/hardware/ITER0-drive-log-analysis-02.md` |
| Iter1 电机链路诊断 | `tests/hardware/ITER1-motor-link-TEST.md` |
| Iter1 电机链路日志 01 | `tests/hardware/ITER1-motor-link-log-01.md` |
| Iter1 电机链路肉眼反馈 01 | `tests/hardware/ITER1-motor-link-feedback-01.md` |
| Iter1 失联停车测试 | `tests/hardware/ITER1-failsafe-TEST.md` |
| Iter1 失联停车日志 01 | `tests/hardware/ITER1-failsafe-log-01.md` |
| Iter1 低速直线循迹测试 | `tests/hardware/ITER1-line-follow-TEST.md` |
| Iter1 低速直线循迹诊断日志 01 | `tests/hardware/ITER1-line-follow-log-01.md` |
| Iter1 普通直线落地测试 | `tests/hardware/ITER1-line-follow-ground-TEST.md` |
| 本次赛道固定路线声明 | `tests/hardware/ITER0-drive-log-analysis-02.md` |
| 旧 5 字节发送协议 | `src/harmony/4.1_Hcsr04_Obstacle_Avoidance/src/car_uart.c` |
| STM32 6 字节解析器 | `src/stm32/7.1_Bluetooth_Control/SYSTEM/usart/usart.c` |
| STM32 PI、PWM、超时与电机引脚 | `src/stm32/7.1_Bluetooth_Control/QST_HARDWARE/SYSTEM_CONTROL/control_system.c` |
| STM32 主调用链 | `src/stm32/7.1_Bluetooth_Control/USER/main.c` |

本文档是 Iter0 的协议基线。后续任何接口、协议、引脚、时序或状态语义变更，必须先更新本文档和对应测试，再修改固件。

## 12. 开发协约与硬件测试交互

### 12.1 责任划分

- 开发者负责：源码、测试、构建目标、烧录对象、操作单、日志格式、结果判读和下一步决策。
- 用户只负责：按操作单烧录/上电/摆放设备、观察是否出现明显危险、从 UartAssistant 原样复制日志或提供截图。
- 用户不需要解释 `0/1`、高低电平、错误码、传感器状态或判断“是否正常”。任何判读由开发者完成。
- 用户遇到电机意外运动、发热、异味、异常声响或无法停止时，先断开电机电源，再反馈现象；不要继续试验。

### 12.2 每轮硬件测试的固定交付物

开发者必须在要求烧录前提供：

1. 唯一的固件目录和 GN target；
2. Hi3861/STM32 分别是否需要烧录；
3. 是否必须架空车轮或断开电机电源；
4. 按编号排列的具体操作动作；
5. 预期日志前缀和完整反馈格式；
6. 异常时的立即停止条件。

用户反馈必须包含：

- 实际烧录的目标（如果失败也原样说明）；
- 每个步骤的原始连续日志，不能手工改写或筛选；
- UartAssistant 截图（文本无法复制时）；
- 仅补充肉眼可见现象，例如“车轮转动/未转动”“日志乱码/清晰”。

### 12.3 证据门禁

- 没有通过主机测试，不进入下一层固件开发。
- 没有获得传感器原始日志，不假设黑线极性、白底极性或传感器安装有效。
- 没有确认单步运动和停止安全，不进入自动循迹。
- 每轮只改变一个可验证变量；日志和源码版本必须能够对应。
- 任何结论都标记为“源码确认”“日志确认”“实车确认”或“待确认”。
- 自动循迹前必须完成：传感器电平取证、车轮方向取证、速度帧取证、失联停车取证、单步运动取证。

### 12.4 测试记录与回滚

- 原始日志保存到 `tests/hardware/`，不得覆盖已有记录。
- 测试固件必须与业务固件分离；诊断固件默认不驱动电机。
- 测试失败时先保留现场和日志，回到最后一个已验证固件；禁止连续叠加未经验证的修改。
- 不因一次日志异常直接修改协议；先检查固件版本、构建 target、接线和日志完整性。
