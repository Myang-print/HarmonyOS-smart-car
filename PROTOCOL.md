# 智能小车接口与协议基线

> 本文定义“黑胶带循迹 + 固定岔路选择 + 死胡同回退 + 终点停车”的稳定接口、硬件事实、安全约束与系统职责。  
> 已确认结论来自当前源码、日志或实车测试；未确认内容必须显式标记，不得由实现自行假设。

---

# 1. 系统架构与职责

```text
黑线/标记传感器 ─┐
HC-SR04（可选） ─┼─> Hi3861
其他上层输入   ─┘      │
                       │ UART2：左右轮目标速度
                       ▼
                    STM32F103
                       │
                       ▼
               PI / PWM / 电机执行
```

## Hi3861

负责：

- 传感器采样与滤波；
- 普通循迹；
- 岔路检测与选择；
- 死胡同识别；
- 路径栈与回溯；
- 起终点识别；
- 高层安全决策；
- 周期发送左右轮目标速度。

## STM32F103

负责：

- UART 速度帧解析；
- 左右轮目标执行；
- 编码器 PI；
- PWM 与电机；
- 本地通信失联停车。

## 强制边界

1. Hi3861 是唯一自主决策者。
2. STM32 不维护第二套迷宫/FSM。
3. 主车控链路统一使用现有 **6 字节左右轮速度协议**。
4. 不得与旧 5 字节离散动作协议混用。
5. 同一时刻只能有一个 Feature 占用 UART2/车控权。
6. 启动、故障、非法输入、通信异常、终点确认等安全状态必须最终输出 `(0,0)`。

---

# 2. Hi3861 → STM32 主车控链路

## 2.1 物理接口

```text
Hi3861 GPIO11 / UART2_TX
        ↓
STM32 PA10 / USART1_RX
```

要求：

- 两板共地；
- 3.3 V TTL；
- `115200-8-N-1`；
- 无硬件流控。

GPIO12 / UART2_RX 当前不是主协议必需接口。

---

## 2.2 6 字节速度协议

固定长度：

```text
FC | LEFT_DIR | LEFT_SPEED | RIGHT_DIR | RIGHT_SPEED | FD
```

字段：

| 字段 | 范围 |
|---|---|
| HEADER | `0xFC` |
| LEFT_DIR | `0/1` |
| LEFT_SPEED | `0..250` |
| RIGHT_DIR | `0/1` |
| RIGHT_SPEED | `0..250` |
| TAIL | `0xFD` |

方向：

```text
0 = 正转
1 = 反转
```

速度单位：

```text
0.01 rad/s
```

编码：

```text
dir   = signed_speed < 0 ? 1 : 0
speed = min(abs(signed_speed), 250)
```

停车：

```text
FC 00 00 00 00 FD
```

---

## 2.3 接收语义

STM32 必须：

- 遇到 `0xFC` 时重新同步；
- 拒绝非法方向值；
- 拒绝速度 `>250`；
- 拒绝错误帧尾；
- 非法帧不得成为新的有效目标；
- 最新合法帧覆盖尚未消费的旧目标。

该协议表达：

> 当前左右轮目标速度。

不表达：

- 动作队列；
- 动作完成；
- ACK；
- 编码器状态；
- 消息顺序保证。

当前协议没有：

- 长度；
- 版本；
- 序号；
- CRC；
- ACK。

---

# 3. 调度与通信安全

## STM32

控制周期：

```text
50 ms
```

通信超时：

```text
2000 ms
```

超过 2 秒无新合法帧：

```text
STOP
```

该失联停车已经通过实车验证。

## Hi3861

业务运行时建议：

```text
50–100 ms
```

刷新一次速度目标。

原则：

- 不应超过 500 ms；
- 电机发送/安全任务不得被阻塞式传感器读取长期饿死；
- `UartWrite()` 必须检查是否完整写入 6 字节；
- 初始化阶段先发送停车；
- 初始化或任务创建失败必须保持停车。

---

# 4. STM32 电机控制基线

当前执行链：

```text
UART command
      ↓
Control_SetRemoteTarget()
      ↓
Control_RemoteStep()
      ↓
Encoder PI
      ↓
PWM
      ↓
Motor
```

主要硬件：

| 功能 | STM32 |
|---|---|
| 左轮方向 | PB14 |
| 左轮 PWM | PB7 / TIM4_CH2 |
| 右轮方向 | PB13 |
| 右轮 PWM | PB6 / TIM4_CH1 |
| 左编码器 | TIM2 |
| 右编码器 | TIM3 |
| PWM | TIM4 / 1 kHz |
| 控制周期 | 50 ms |

原则：

- 自主车控通过 `Control_SetRemoteTarget()`；
- Hi3861 协议路径不得绕过 PI 直接控制底层 PWM；
- 速度、差速、转弯参数必须实车标定；
- STM32 本地阻塞式 90° 转弯接口不属于当前 6 字节协议；
- 迷宫控制不得混合“Hi3861 持续速度控制”和“STM32 独立阻塞式导航动作”。

---

# 5. 黑胶带传感器基线

## 5.1 接口

| 位置 | Hi3861 |
|---|---|
| 左红外 | GPIO13 |
| 右红外 | GPIO14 |

GPIO 输入读取。

## 5.2 已确认极性

当前硬件和安装条件下：

```text
0 = 白色道路包边
1 = 黑胶带或已经越过白色包边
```

该语义已经实机确认。

硬件、接线或安装变化后必须重新验证。

---

## 5.3 双传感器基本语义

```text
L R

0 0   普通居中状态
1 0   左侧检测黑带 → 向左修正
0 1   右侧检测黑带 → 向右修正
1 1   歧义状态
```

其中 `11` 可能意味着：

- 两探头进入特殊宽黑区域；
- 一个探头进入黑带，另一探头越过白色包边；
- 岔路或特殊标记。

因此：

> `11` 不能单独被定义为岔路、死路、终点或某个固定运动动作。

---

# 6. 传感器处理原则

正式算法必须基于：

```text
连续采样
+
滤波/消抖
+
事件时序
+
FSM 状态
```

而不能仅依据单次 `L/R` 状态。

当前可用起点：

```text
采样周期：20 ms
事件确认：连续 3 个一致样本
```

要求：

- 读取失败立即进入安全停车；
- 不允许无限沿用旧传感器值；
- 特殊标记必须有进入/处理/离开状态；
- 同一物理事件离开前只能处理一次。

若实车数据证明双传感器无法区分必要事件：

> 停止继续增加算法复杂度，应增加传感器或调整赛道标记/安装。

---

# 7. 赛道事实

## 普通道路

- 中央为黑胶带；
- 两侧为白色包边；
- 两探头整体横向覆盖宽度略大于中央黑胶带。

## Y 字岔路

本次赛道固定策略：

```text
第 1 个 Y 字岔路 → 左
第 2 个 Y 字岔路 → 右
```

这是固定赛道策略，不是：

- 左手法则；
- 右手法则；
- 随机搜索规则。

岔路不得由瞬时 `10/01/11` 单独决定。

---

## 死胡同

赛道上的死路标记表现为：

> 垂直于当前行驶路径的单条黑色横带。

但必须通过实车时序确认其可识别性，不能仅依据静态图片建立算法事实。

---

## 起点与终点

起点和终点均为完整“干”字型标记：

```text
     横线
─────┼─────
     │
─────┼─────
     │
   纵向路径
```

关键事实：

- 两条横线属于同一个完整标记；
- 横线之间存在白色间隔；
- 中央纵向黑线连续贯穿；
- 不能把两条横线分别理解为起点和终点。

终点必须由完整事件时序确认。

---

# 8. 高层行为语义

目标 FSM 至少包含：

```text
CALIBRATING
    ↓
FOLLOW_LINE
    ↓
APPROACH_JUNCTION
    ↓
CENTER_ON_JUNCTION
    ↓
CHOOSE_BRANCH
    ↓
EXECUTE_TURN
    ↓
LEAVE_JUNCTION
    ↓
FOLLOW_LINE
```

以及：

```text
DEAD_END
   ↓
U_TURN
   ↓
BACKTRACK
```

安全终态：

```text
SENSOR_FAULT
LINK_FAULT
ACTION_TIMEOUT
FINISHED
      ↓
STOPPED
```

---

# 9. 岔路与路径状态

一个岔路节点至少需要保存：

- 到达顺序/身份；
- 进入方向；
- 可选出口；
- 已尝试出口；
- 当前选择结果。

不能只存：

```text
LEFT / RIGHT
```

而忽略车辆进入方向。

同一岔路：

> 从进入检测区域到明确离开，只允许处理一次。

---

# 10. 死胡同与回溯

确认死胡同后：

```text
STOP
 ↓
180° 掉头
 ↓
沿原路径循迹返回
 ↓
寻找最近仍有未探索出口的岔路
 ↓
选择下一出口
```

路径管理至少需要：

```text
拓扑栈
+
相对方向转换
```

禁止使用：

```text
按固定时间倒车
```

作为“回到岔路”的主要机制。

没有剩余出口的节点从路径栈中移除。

---

# 11. 终点语义

终点必须识别完整时序，例如：

```text
普通路径
   ↓
第一横线事件
   ↓
中间纵线 / 白色间隔阶段
   ↓
第二横线事件
   ↓
离开完整标记
   ↓
FINISHED
```

还必须结合运行阶段区分：

```text
START marker
FINISH marker
```

确认终点以后：

```text
clear motion intent
↓
target = (0, 0)
↓
持续发送停车
↓
STOPPED
```

终点停车为永久状态。

不得由于：

- 超时；
- 新传感器输入；
- 后续普通黑线；

自动恢复运动。

---

# 12. 动作安全

以下动作必须有最大持续时间：

- 循线恢复；
- 路口居中；
- 转向；
- U-turn；
- 回溯。

动作超过规定时间：

```text
ACTION_TIMEOUT
↓
STOPPED
```

禁止无限：

- 前进；
- 转圈；
- 倒车；
- 保持无法解释的旧动作。

具体超时值属于实车标定参数，不属于稳定接口协议。

---

# 13. HC-SR04 可选安全传感器

Hi3861：

```text
Trig = GPIO7
Echo = GPIO8
```

当前驱动：

- Trig 高电平约 12 µs；
- 边沿等待阶段各 30 ms 超时；
- 合法距离 `2–400 cm`；
- 最多尝试 5 次；
- 最多 3 个有效值取中位数；
- 样本间隔约 60 ms。

距离：

```text
distance_cm = echo_us × 0.0343 / 2
```

由于读取可能阻塞数百毫秒：

> HC-SR04 不得运行在高优先级电机发送任务内。

应独立采样并发布带时间信息的结果。

结果：

- 过期；
- 连续失败；
- 驱动异常；

必须进入安全处理。

Echo 电气电平仍需确认不超过 Hi3861 输入允许值。

---

# 14. 当前架构能力边界

当前系统存在以下已知限制：

1. Hi3861 → STM32 为单向通信。
2. 没有 ACK。
3. 没有 CRC。
4. 没有消息序号。
5. Hi3861 无法直接知道 STM32 是否真正执行动作。
6. STM32 不回传编码器。
7. STM32 不回传电机故障。
8. Hi3861 无法直接基于编码器确认转向完成。
9. 双路红外可能不足以无歧义识别所有特殊赛道事件。
10. 当前没有绝对定位或地图节点 ID。

因此当前 Hi3861 决策原则只能主要依赖：

```text
地面传感器事件
+
FSM
+
拓扑次序
+
动作时间约束
```

不得假设当前不存在的编码器反馈。

---

# 15. 未来协议升级

如果后续需要真正的动作闭环，应新建版本化协议。

建议未来加入：

- version；
- length；
- message type；
- sequence；
- payload；
- CRC；
- ACK；
- encoder count；
- STM32 state；
- fault code。

不得在不改变协议版本的情况下偷偷修改当前 6 字节帧语义。

---

# 16. 旧 5 字节协议

旧避障协议：

```text
A5 5A SEQ CMD XOR
```

属于独立旧工程：

- 不同 GPIO；
- 不同 UART；
- 不同波特率；
- 不同 STM32 固件；
- 不同控制语义。

因此：

> 仅保留历史兼容记录，正式综合车控禁止使用。

---

# 17. 构建规则

## Hi3861

- `src/harmony/` 是 OpenHarmony 应用快照，不是完整源码树；
- 必须同步到完整 OpenHarmony tree；
- 同一时刻仅启用一个占用车控资源的 Feature；
- 使用对应 OpenHarmony 版本构建；
- build success 只代表编译/链接成功。

## STM32

- 使用对应工程重新 Rebuild；
- 烧录本次源码产生的新 HEX；
- build success 不代表运动方向、速度或控制行为已经实车验证。

---

# 18. 安全不变量

任何实现必须始终满足：

### S1

```text
初始化失败 → STOP
```

### S2

```text
传感器读取失败 → STOP
```

### S3

```text
非法协议 → STOP / 不接受新目标
```

### S4

```text
通信失联 → STM32 ≤ 2 s 停车
```

### S5

```text
动作超时 → STOP
```

### S6

```text
FINISHED → 永久 STOP
```

### S7

无法可靠识别当前环境时：

```text
不得通过无限运动“猜测”状态
```

---

# 19. 实验与证据规则

结论统一分为：

```text
[SOURCE]   源码确认
[LOG]      日志确认
[HW]       实车确认
[PENDING]  待确认
```

开发原则：

1. 没有主机测试通过，不进入下一层。
2. 没有原始日志，不推断传感器事实。
3. 没有单步运动安全确认，不进入自动运行。
4. 每轮实验原则上只改变一个可验证变量。
5. 日志必须与源码/固件版本对应。
6. 原始硬件日志不得覆盖。
7. 失败后返回最后一个已验证状态，不连续叠加未经验证的 patch。
8. 测试固件与正式业务固件分离。
9. 诊断固件默认不主动驱动电机。

---

# 20. 硬件测试交互规范

每次要求实车测试前必须明确：

- 烧录哪个 Hi3861 Feature；
- STM32 是否需要重新烧录；
- 是否架空车轮；
- 是否断开电机电源；
- 操作步骤；
- 预期日志；
- 用户需要返回的原始信息；
- 危险停止条件。

用户只负责：

- 烧录；
- 上电；
- 按操作单摆放/操作；
- 返回原始日志；
- 返回截图；
- 描述肉眼可见现象。

协议、电平、状态和错误含义由开发者判断。

---

# 21. 当前已经确认的底层能力

以下能力已经作为后续开发基线：

- Hi3861 UART2 初始化成功；
- GPIO11 → STM32 速度协议链路可用；
- 6 字节速度帧可驱动车轮；
- 前进有效；
- 后退有效；
- 左右差速/转向有效；
- 停车帧有效；
- STM32 约 2 秒失联停车有效；
- GPIO13/14 红外读取可用；
- 当前红外极性已实机确认。

这些底层事实原则上不因上层循迹算法失败而重新设计。

---

# 22. 当前开发范围

最终目标：

```text
起点
 ↓
稳定黑线循迹
 ↓
第 1 Y 岔路选左
 ↓
继续循迹
 ↓
第 2 Y 岔路选右
 ↓
必要时识别死胡同
 ↓
U-turn + 回溯
 ↓
重新选择有效出口
 ↓
识别完整终点
 ↓
永久停车
```

但开发必须逐层完成。

当前最近目标首先是：

```text
普通黑色直线路段
        ↓
稳定、连续、安全循迹
```

在普通直线循迹通过实车验收之前，不进入：

- Y 字岔路；
- 死胡同；
- 回溯；
- 起终点识别；
- 完整迷宫 FSM。

---

# 23. 不属于本稳定协议的内容

以下内容必须放入具体 Iteration Spec，而不是作为长期协议事实：

- `00/10/01/11` 的具体速度值；
- BASE_SPEED；
- correction gain；
- `11` recovery policy；
- `11` timeout；
- SPIN_RECOVERY；
- 特定转向动作算法；
- 90°/180° 参数；
- 事件阈值；
- 滤波计数；
- 路口居中时间/距离；
- Iter1 当前候选实现。

这些都是：

```text
可实验
可修改
可回滚
```

的算法策略，而不是稳定硬件/协议事实。